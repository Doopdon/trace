function installingTrace(){
    return div([
        h4('It really is as easy as 1,2,3....4'),
        div([
            p('Click the copy button'),
        ]),
        div([
            p('Put the code in a script tag')
        ]),
        div([
            p('call the function "traceInit(window)"')
        ]),
        div([
            p('run this code and give it the id of an element you want to add the rendered elements to.')
        ]),

    ])
}